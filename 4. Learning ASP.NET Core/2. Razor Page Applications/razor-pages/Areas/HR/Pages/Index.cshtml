@* Created using: dotnet new page --name Index --namespace razor_pages.HR --output Areas\HR\Pages *@
@page "{name?}/{id?}" // Added route template
@model razor_pages.HR.IndexModel
@{
}

<h1>Greeting from the HR team</h1>

@if (@Model.Name is not null) {
    <h3>Welcome @Model.Name! Your employee ID is @Model.Id. </h3>
}